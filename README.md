# value-at-risk-expected-shortfall

pip freeze | grep -E "yfinance|pandas|numpy|matplotlib" > requirements.txt

cat > README.md <<'MD'

Simple VaR and Expected Shortfall calculator using daily returns (yfinance).

## Usage
```bash
python3 var_calculator.py
سپس:
```bash
git add README.md requirements.txt
git commit -m "Add README and requirements"
git push
